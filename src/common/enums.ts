export enum Directions {
  NORTH = "N",
  SOUTH = "S",
  EAST = "E",
  WEST = "W",
}

export enum DirectionVectors {
  FORWARD = 1,
  BACKWARD = -1,
  FIXED = 0,
}

export enum Actions {
  LEFT = "L",
  RIGHT = "R",
  MOVE = "M",
}
